# Add  code here!

def prime?(max)
  list = (2..max).to_a 
  prime_list = []
  
  while value = list.shift
    prime_list << num 
    if value = Math.sqrt(max).floor
      break
      if list.each do {|num| num % value ==0}
      return false  
    end
  
  
end
